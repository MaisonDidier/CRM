SELECT cron.unschedule('relances-quotidiennes');

SELECT cron.schedule(
  'relances-quotidiennes',
  '50 9 * * *',
  $$
  SELECT net.http_post(
    url := 'https://crm-sable-two.vercel.app/api/relances/send',
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'Authorization', 'Bearer 2GWnyAlcUoIr9W7SAU+LOXbqujyvyFCVycHhEwWCmV8='
    ),
    body := '{}'
  );
  $$
);
